#!/bin/sh

curl -ssl https://s3.amazonaws.com/wookie-buildpack/config.admin.js -O ../config.admin.js
curl -ssl http://s3.amazonaws.com/config.dev.js -O ../config.dev.js

mkdir -p tmp

npm install

node build
